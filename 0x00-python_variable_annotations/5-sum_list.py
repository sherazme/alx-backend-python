#!/usr/bin/env python3
""" list of floats """
from typing import Callable, Iterator, Union, Optional, List


def sum_list(input_list: List[float]) -> float:
    """
    Takes list of floats input_list as argument
    returns their sum as a float
    """

    return sum(input_list)
